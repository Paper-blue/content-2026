# 推送前门控状态

## 传播优化
- 标题：OpenAI换驾驶舱，荣耀京东签千亿合作，英特尔押注先进封装
- 摘要：Anthropic收入到300亿；谷歌离线听写；GEN-1到99%；AWS上架Agent评估；Accio改写选品
- 评估：信息量足、前三条主题清晰，综合传播力约 **8/10**。

## 敏感审核
- 结果：通过
- 说明：未发现高危/中危敏感表达。

## AI 检测
- 结果：跳过
- 原因：当前会话内未读取到 `TENCENT_SECRET_ID` / `TENCENT_SECRET_KEY`，`ai-detect-gate.py` 按缺凭证跳过态处理。

## 微信推送
- 结果：失败
- 原因：微信接口返回 `errcode 40164`，当前出口 IP `116.149.153.112` 不在公众号白名单内。
- 已尝试：
  1. 直连发布
  2. 按 `wechat.env` 加载 SOCKS 代理后重试
  3. 为代理环境补装 `PySocks` 后再次重试
- 结论：内容产物已全部生成，但草稿箱推送被微信白名单限制阻断。
